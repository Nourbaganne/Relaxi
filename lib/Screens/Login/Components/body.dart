import 'package:flutter/cupertino.dart';
import 'package:relaxy_pfa_project/Screens/Login/Components/background.dart';

class Body extends StatelessWidget {
  const Body({
    Key key,
  }) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Background(
      child: Column(),
    );
  }
}
